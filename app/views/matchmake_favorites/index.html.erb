<h2>マッチメイク お気に入り一覧</h2>
<div class="row">
  <div class="col-10 offset-1">
    <table class="table">
      <thead>
        <tr>
          <th>投稿タイトル</th>
          <th>投稿日</th>
          <th></th>
        </tr>
      </thead>
      <% @favorites.each do |favorite| %>
        <tbody>
          <tr>
            <td>
              <%= favorite.matchmake.title %>
            </td>
            <td><%= favorite.matchmake.created_at.strftime("%Y年%m月%d日") %></td>
            <td>
              <%= link_to "詳細へ", matchmake_path(favorite.matchmake_id) %>
            </td>
          </tr>
        </tbody>
      <% end %>
    </table>
  </div>
</div>